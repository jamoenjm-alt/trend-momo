# Session log — 2026-07-08 (Claude Fable)

Portable handoff: everything another AI model (or future session) needs to continue.
Read with PROJECT-BRIEF.md (context) and ROADMAP.md (direction).

## What was found

1. **Folder had silently rolled back to Jun 30 file versions** (file-sync restore, not git).
   Working tree was a week behind HEAD; if the daily .bat had run, it would have force-pushed
   the rollback to the live site. Restored all 10 affected files from HEAD (`git show HEAD:f > f`).
   A git stash attempt during recovery corrupted the index (mount forbids unlink); fixed after
   enabling file deletion — repo verified healthy (`git fsck` clean, index settled).
2. **Two competing daily updaters**: GitHub Action (22:00 UTC) AND a local Windows Scheduled
   Task both bake+push daily → the merge-collision commits in the log. Action kept as primary;
   local task should be disabled by Jimmy (can't be done from the sandbox).
3. **GitHub Action bake was stale** — prices.json data stopped at 2026-07-06. Check the
   Actions tab; possibly Yahoo blocking runner IPs or a failed push.
4. **Live-site QA: clean.** No console errors; all columns render.
5. Cowork-specific trap: when a tool shrinks a file on this mount, trailing NUL bytes can be
   left behind (hit update-prices.py and the workflow yml; both scrubbed). Check after big edits.

## What was changed (all tested: py_compile, node --check, signal-math smoke test on real data)

1. **Data split** — `data/prices.json` now holds `{updated, prices, pe, ohlc, weekly}`;
   the HTML fetches it at boot (`loadStaticData()` at the bottom of the script) and fell
   from 543 KB to ~200 KB. `update-prices.py` rewritten to read/write the JSON atomically
   with merge semantics (failed fetches keep yesterday's data). CSV-import mode updated too.
   New `serve.bat` for local viewing (file:// blocks the JSON fetch).
2. **Universe expanded to 274 rows**, all constituent lists researched fresh on 2026-07-08:
   US Top 100 (stockanalysis.com; BHP/RIO excluded → they live in ASX), ASX 50, HK 30
   (junk cross-listings excluded), Crypto 50 (slickcharts; ex-stablecoins; MATIC→POL with
   price history migrated, TON→"Gram", Maker→SKY; unverifiable new tokens CC/M/ASTER/WLFI
   skipped), Commodities 20 (honest liquid set — a "top 50 commodities" does not exist).
3. **Navigation**: "All" tab removed; Watchlist is the default tab; searching shows matches
   from every section regardless of active tab. Labels: US Top 100 / ASX 50 / Hong Kong 30.
4. **Workflows hardened**: daily .bat and Action now `git add` only the three data/site files
   (was `add -A`); claude-publish.bat takes the commit message as an argument (was a
   hardcoded stale message); Action commits data/prices.json.
5. **Docs**: CLAUDE.md rewritten (was describing the 33-ticker era), ROADMAP.md created,
   this session log created. PROJECT-BRIEF.md updated.

## Immediate actions for Jimmy

1. ~~Delete .git lock files~~ — done during the session.
2. **Run `claude-publish.bat feat: data split + 274-ticker universe`** to publish.
3. **Trigger the price bake**: GitHub → Actions → Daily Price Update → Run workflow
   (or `python update-prices.py` locally, ~12 min). Until then most new tickers show N/A.
4. **Check why the Action skipped Jul 7–8**, and **disable the Windows Scheduled Task**
   for update-prices-daily.bat (Task Scheduler) so there is one daily writer.
5. Open the live site after bake: confirm ASX 50 / HK 30 / new crypto rows fill in.
   Any crypto stuck on N/A after a bake = wrong CoinGecko id (see ROADMAP "Data debt").

## Known regressions accepted

- A2M (a2 Milk) dropped from ASX section (below top 50). Re-add via + ADD TICKER if wanted.
- Old long-tail cryptos (MANA, SAND, GRT, EGLD, DYDX, RUNE, LDO, STX, FTM, FLOKI, WIF, OP,
  MKR, TRX-adjacent alts) dropped in favour of the current top 50. Their baked history
  remains in data/prices.json harmlessly.
- Opening regime-board.html by double-click no longer shows baked data (file:// CORS) —
  use serve.bat or the live site. Live fallbacks still work either way.
